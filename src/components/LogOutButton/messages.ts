import { defineMessages } from "react-intl";

export const messages = defineMessages({
  closeSession: {
    id: "components.LogOutButton.closeSession",
    defaultMessage: "Cerrar sesión",
  },
});
