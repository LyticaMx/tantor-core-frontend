import { defineMessages } from "react-intl";

export const modalMessages = defineMessages({
  newFolder: {
    id: "components.FileTree.FolderModal.newFolder",
    defaultMessage: "Nueva carpeta",
  },
});
