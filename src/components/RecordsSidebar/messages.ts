import { defineMessages } from "react-intl";

export const messages = defineMessages({
  searchRecord: {
    id: "components.RecordsSidebar.searchRecord",
    defaultMessage: "Buscar expediente",
  },
});

export const modalMessages = defineMessages({
  newRecord: {
    id: "components.RecordsSidebar.RecordsModal.newRecord",
    defaultMessage: "Nuevo expediente",
  },
});
